# Azure App Service Environment Configuration
# Copy these settings to your Azure App Service Configuration -> Application settings

# Node.js settings
WEBSITE_NODE_DEFAULT_VERSION=20.18.1
SCM_DO_BUILD_DURING_DEPLOYMENT=true

# App settings
NODE_ENV=production
PORT=80

# Enable detailed error pages (for debugging, remove in production)
WEBSITE_DETAILED_ERROR_MESSAGES=true
WEBSITE_NODE_DEFAULT_VERSION=20.18.1

# Startup command (alternative to web.config)
# In Azure portal: Configuration -> General settings -> Startup Command
# node app/server/dist/index.js

# Build commands for Kudu/SCM
# These will run during deployment if SCM_DO_BUILD_DURING_DEPLOYMENT=true
# 1. npm ci (installs dependencies)
# 2. npm run build (builds the app)

# Environment variables for your app (add these in Azure portal)
# OPENAI_API_KEY=your_openai_api_key_here
# SERP_API_KEY=your_serp_api_key_here
# UNSPLASH_ACCESS_KEY=your_unsplash_key_here
# COSMOS_DB_CONNECTION_STRING=your_cosmos_connection_string_here
